---
title: 'Skybox 3D'
---

You can easily create dynamic gui 3D scenes by declaring a `guiManager` and then
3D components (like `holographicButton`) will automatically be added.

```tsx
<guiManager>...</guiManager>
```

[devtool:Skybox3D.tsx]
