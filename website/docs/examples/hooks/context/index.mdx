---
title: 'Context'
---

If you want to access the context directly follow this code and watch the
logging. It's easier to use the convenience hooks
`useScene`/`useEngine`/`useCanvas`.

[devtool:Context.tsx]
