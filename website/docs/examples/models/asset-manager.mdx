---
title: 'Asset Manager'
---

There is a `useAssetManager` convenience hook. Wrap it in a `<Suspense .. />`
see the example for full details - this example uses a fallback component to
track loading progress.

<code src="./asset-manager/AssetManager.tsx" />
