{
  "name": "react-babylonjs",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "lint": "yarn workspaces run lint"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "prettier-plugin-organize-imports": "^2.3.4"
  }
}
