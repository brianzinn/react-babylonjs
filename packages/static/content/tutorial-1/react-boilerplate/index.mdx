---
title: 'Creating a Boilerplate React Project'
sort: 10
---

import Component_HelloReact_g1642459759807 from './HelloReact'
import Devtool_HelloReact_g1642459759808 from '!!@devtool/loader!./HelloReact'
import { DevTool } from '@devtool/react'

If you already know how to create a React project, you can skip this section.
Otherwise, let's go!z

```bash
npx create-react-app my-app
```

or with yarn:

```bash
yarn create react-app my-app
```

Once you have a project, you'll find a setup that looks a little like this:

<code>{JSON.stringify(Devtool_HelloReact_g1642459759808, null, 2)}</code>

<DevTool
  prefix={'g1642459759809'}
  component={Component_HelloReact_g1642459759807}
  meta={Devtool_HelloReact_g1642459759808}
/>
