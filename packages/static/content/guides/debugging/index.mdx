---
title: 'Debugging'
---

It would be very cool if we had a way to interact with the application state for
debugging purposes.

Let's use the `@babylonjs/inspector` NPM package: This breaks gatsby (SSR?)
since 'document' is not found... --devtool:Debug.tsx--
